# 🏠 Інструкція з налаштування житла у Берліні

## ✅ Що вже зроблено:

1. **Код оновлено** - форма тепер працює без обов'язкової авторизації
2. **Навігація додана** - кнопка "Всі оголошення" на головній сторінці веде на сторінку житла
3. **Відображення налаштовано** - житло відображається на головній сторінці та на сторінці `/housing`

## 🔧 Що потрібно зробити в Supabase:

### Крок 1: Виконати SQL міграцію

1. Відкрийте ваш проект в [Supabase Dashboard](https://supabase.com/dashboard)
2. Перейдіть в **SQL Editor**
3. Натисніть **"New query"**
4. Відкрийте файл `fix_housing_user_id.sql` з корня проекту
5. Скопіюйте весь вміст і вставте в SQL Editor
6. Натисніть **"Run"** (або `Ctrl/Cmd + Enter`)

✅ Це додасть колонку `user_id` до таблиці `housing` (якщо вона відсутня) та налаштує правильні RLS політики.

### Крок 2: Перевірити таблицю

1. В Supabase Dashboard перейдіть в **Table Editor**
2. Виберіть таблицю `housing`
3. Переконайтесь, що колонка `user_id` існує (тип: `uuid`, nullable: `true`)

### Крок 3: Перезапустити додаток

```bash
npm run dev
```

## 🎯 Як це працює:

### Додавання оголошення:
1. Натисніть кнопку **"+"** (FAB) в правому нижньому куті
2. Оберіть **"Опублікувати житло"**
3. Заповніть форму (обов'язкові поля позначені *)
4. Натисніть **"Додати"**

✅ Оголошення збережеться в Supabase, навіть без авторизації (user_id буде `null`)

### Перегляд оголошень:

**На головній сторінці:**
- Компонент "Житло" показує перші 2 оголошення
- Кнопка "Всі X оголошень →" веде на повну сторінку

**На сторінці `/housing`:**
- Всі оголошення з фільтрацією за категоріями
- Пошук за назвою, районом або описом
- Фільтри: Всі, Квартири, Кімнати, Студії, Будинки

## 📋 Структура даних:

Оголошення про житло містить:
- **title** - заголовок (обов'язкове)
- **type** - тип: apartment, room, studio, house
- **price** - ціна в € (обов'язкове)
- **size** - площа в м² (опціонально)
- **rooms** - кількість кімнат (опціонально)
- **address** - адреса (обов'язкове)
- **district** - район (опціонально)
- **description** - опис (обов'язкове)
- **contact_name** - ім'я контакту (опціонально)
- **contact_email** - email (опціонально)
- **contact_phone** - телефон (обов'язкове)
- **user_id** - ID користувача (опціонально, може бути null)

## ❓ Troubleshooting

### Помилка "Could not find the 'user_id' column"
- Переконайтесь, що ви виконали SQL міграцію `fix_housing_user_id.sql`
- Перевірте в Table Editor, що колонка `user_id` існує

### Оголошення не з'являються після додавання
- Перевірте консоль браузера на помилки
- Переконайтесь, що RLS політики дозволяють INSERT (див. `fix_housing_user_id.sql`)
- Перезавантажте сторінку

### Форма не відправляється
- Перевірте, що всі обов'язкові поля заповнені (*)
- Перевірте консоль браузера на помилки Supabase
- Переконайтесь, що `.env` файл містить правильні Supabase credentials

---

**Готово!** Тепер користувачі можуть додавати та переглядати оголошення про житло! 🎉
